# variable of 'matriz'.
matriz = [[0, 0, 0], 
          [0, 0, 0], 
          [0, 0, 0]]

# format the 'matriz'.
for linha in range(0,3):
    for colun in range(0,3):
        matriz[linha][colun] = int(input('Number '))

# print the 'matriz'.
for c in matriz:
    print(c)

# i needed to use AI for this lesson. 20/03/23